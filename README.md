# Lightweight Hybrid Retrieval on a Unified Candidate Space — Reproduction Bundle

Reproduction materials for an offline evaluation of hybrid retrieval in a local
(desktop, on-device) RAG stack: SQLite FTS5 trigram BM25, BGE-M3 dense scanning,
Reciprocal Rank Fusion with deterministic tie-breaking, and an anti-starvation
per-source quota — compared over one and the same entity set.

The manuscript is under review and is **not** part of this repository. What is
published here is the frozen benchmark input, the code that turns it into every
reported number, and the figures.

---

## Why a "unified candidate space"

In the deployed configuration only the user's private chunks carry a BM25 index;
built-in knowledge nodes and questions are reachable through the dense channel
only. Scoring the two channels against each other on those terms conflates
*corpus visibility* with *ranking mechanism*: a fusion gain could just be an
index-coverage gain.

So every entity is mirrored into a single index (1,696 entities), both channels
are run over that identical set, and the deployment-path numbers are reported
separately as a source-policy reference rather than as a method comparison.

---

## Repository layout

```
work/                     frozen, pseudonymised benchmark inputs (never regenerated here)
  qa_100_human_v3.jsonl   100 base questions, pooled candidate votes, gold labels
  hybrid_raw_a20_*.jsonl  frozen deployment-path rankings, Top-20 per channel, both forms
  fair_rank_*.jsonl       frozen Rust-harness rankings (base / unified x template / bare)
  annotation/             per-rater exports, majority votes, kappa report
benchmark-results/
  20260916-221246-.../    frozen aggregates: results_overall.csv, paired_bootstrap.csv,
                          per_query_metrics.csv
src-tauri/src/rag/        hybrid.rs, eval.rs — the Rust retrieval core the numbers
                          were measured from
results/                  regenerated by the stages below
figures/                  regenerated vector + raster figures
*.py                      the pipeline (see next section)
LICENSE                   MIT, covering the code and the pseudonymised assets
```

Figure files keep their working names, which do not match the manuscript's figure
numbers: `f6_quota_ablation` is the quota figure, `f5_coverage_vs_fusion` the
coverage one, `f1_bootstrap_forest` the interval plot, and `f2_gold_sensitivity`
the sensitivity grid. The architecture schematic is not included — it belongs to
the manuscript rather than to the measurement.

## Requirements

Python 3.10+ with `matplotlib` (which pulls in `numpy`); everything else is
standard library. No GPU, no network, no model inference — the embeddings and
rankings are frozen assets.

```bash
pip install matplotlib
```

## Quick start

```bash
python reproduce.py        # re-run every stage, diff against the checked-in artefacts
python check_release.py    # assert no non-pseudonymised material slipped back in
```

`reproduce.py` compares figures as PNG bytes, so a clean run leaves the tree
unmodified; the SVG, PDF and `figures_manifest.json` stamp a generation date and
will show as touched even though nothing measured changed.

Or stage by stage, in this order:

| # | Command | Produces |
|---|---------|----------|
| 1 | `python fair_candidate_space.py report` | `benchmark-results/<timestamp>-rag-fair-candidate-space/` — Table 3 aggregates, Table 4 paired bootstrap, per-query metrics |
| 2 | `python quota_ablation.py` | `results/quota_ablation.txt` — the Section 6.1 quota on/off ablation |
| 3 | `python gold_sensitivity_ext.py` | `results/gold_sensitivity_ext.{json,txt}` — the Section 6.6 gold-label sensitivity |
| 4 | `python revision_recompute.py` | `results/revision_recompute.json` — pooled-formulation intervals, per-metric oracle upper bound, source exposure |
| 5 | `python make_figures.py` | `figures/*` — every figure, read from the artefacts above |

`python fair_candidate_space.py prepare` and `rank` are **not** runnable from this
checkout: they rebuild the SQLite mirrors from the private knowledge base and drive
the Rust harness. See [What is not reproducible here](#what-is-not-reproducible-here).

## Data and pseudonymisation

Entity identifiers in the source project are corpus-derived slugs — dictionary
head words for built-in nodes, and content fingerprints for private chunks. They
are replaced here by `desensitize.py` with opaque ids that keep the source-space
prefix (`knowledge_node:kn-#####`, `question:qs-#####`, `private_chunk:pc-#####`),
so the fusion code's prefix test and the tie-break's final key still behave.

Two properties make the replay exact, and both are checked:

* ids are numbered in **sorted order of the original key** within each prefix, so
  lexicographic key order — the last component of the tie-break — is preserved;
* the evaluation mirror ids (`…evalmirror-kn-…`) are **folded back onto the entity
  they mirror before** rewriting, otherwise one entity would receive two ids.

1,111 distinct entity ids appear in the published assets. Additionally stripped:
the free-text `query` / `target_term` fields (set to `null`) and the reviewer-note
wording in `ai_review_correction` / `ai_review_supplement` (replaced by
`[redacted: corpus-derived text]`), the query text inside `multi_kappa_report.json`,
and the pre-anonymisation mirror fingerprints. The reverse mapping is not
published and cannot be recovered from these files.

Reviewers are recorded under the role labels `评审一…评审六` (annotator 1…6) with no
personal identifier; two are human experts, three are model raters, and one was
dropped for being a near-duplicate of another. The `annotator` field keeps that
label because `gold_annotation_sensitivity.py` keys on it rather than on the file
name.

### Known disclosure

`src-tauri/src/rag/hybrid.rs` is published **byte-for-byte as measured**, including
its unit tests, whose fixtures are short illustrative Chinese phrases (for example
a string about the intuitive meaning of a limit). Those are test inputs for the
FTS5 trigram path, not corpus passages, but they are Chinese educational wording and
are therefore called out rather than silently edited: altering them would mean
shipping reference code that was never compiled.

## Headline numbers reproduced from these assets

All over `conditional_93` — the 93 base questions with at least one relevant entity
in the pooled candidate set, 186 query instances with both formulations pooled.

| Quantity | Value | Artefact |
|---|---|---|
| RRF (k=60, no quota) NDCG@5 / MRR@5 | 0.8916 / 0.9330 | `results/revision_recompute.json` → `levels_both` |
| ΔNDCG@5 vs Dense | +0.1124, 95% CI [+0.0555, +0.1756] | ditto → `both` |
| ΔNDCG@5 vs BM25 | +0.1781, 95% CI [+0.1350, +0.2232] | ditto |
| Per-metric max(BM25, Dense) oracle NDCG@5 | 0.8993 | ditto → `levels_both.oracle` |
| ΔNDCG@5 RRF − oracle | −0.0077, 95% CI [−0.0256, +0.0095] | ditto → `both` |
| Quota replay fidelity | 186/186 lists reproduced key for key, score for score | `results/quota_ablation.txt` |
| Quota@20 vs no quota at depth 20 | identical for 186/186 | ditto |
| Quota@5 vs no quota | changes the 5-slot list for 103/186 | ditto |
| ΔNDCG@5 (no quota − quota@5) | +0.0215, 95% CI [+0.0025, +0.0402] | ditto |
| ΔMRR@5 / ΔHit@5 (no quota − quota@5) | −0.0030 [−0.0094, +0.0022] / −0.0108 [−0.0376, +0.0108] | ditto |
| Gold-label sensitivity, `majority5` | 93 questions, 240 gold items | `results/gold_sensitivity_ext.json` |
| … vs `human_unanimous_pos` | 84 questions, 155 gold items, 52 items differ | ditto |
| Private share of Top-5 slots (RRF / Dense / BM25) | 0.1903 / 0.1720 / 0.0925 | `results/revision_recompute.json` → `exposure` |

The intervals are percentile bootstrap resamples clustered at the level of the
base question (2,000 draws), so the two formulations of one question always move
together. They are item-wise intervals and are **not** corrected for multiple
comparisons.

### Seeds

| Stage | Base seed | Actual draw |
|---|---|---|
| `fair_candidate_space.py report` | 20260916 | `Random(SEED + 17)` = 20260933 |
| `quota_ablation.py` | 20260916 | `BOOT_SEED = 20260916 + 13` = 20260929 |
| `gold_sensitivity_ext.py` | reuses the `report` stream | 20260933 |

## What is not reproducible here

Stated plainly, because "reproduces" should mean something:

* **Retrieval itself.** The corpus (built-in educational dictionary entries plus a
  private lecture/exam document set) is not published, so the BM25 and dense
  channels cannot be re-run and the ranking files here are the frozen inputs, not
  re-derivable outputs. `prepare` and `rank` will fail on a public checkout by
  design.
* **Annotation.** The 847 pooled query–entity judgements were collected once
  through a blind annotation page; re-running the pipeline re-uses those labels, it
  does not recreate them.
* **The oracle row in `results_overall.csv`.** `oracle_best_single_channel` compares
  only the first hit position and assigns NDCG@5 = 1 to any hit, which is not a
  valid upper bound. Stage 4 recomputes the per-question, per-metric
  `max(BM25, Dense)` bound instead, and that is the number worth quoting.
* **Corpus-derived wording.** Because `query` and `target_term` are redacted, the
  alignment between a question and its intended dictionary entry is not externally
  checkable from this bundle, only the labels and rankings.

`reproduce.py` asserts the stronger claim that *is* supported: given `work/`, each
stage regenerates the checked-in artefacts unchanged.

## Licence

MIT, see [LICENSE](LICENSE). It covers the pipeline scripts, the Rust reference
sources and the pseudonymised benchmark assets in this repository. The underlying
corpus is not published and is not licensed by this file; neither is the
manuscript, which is under review and lives elsewhere.
